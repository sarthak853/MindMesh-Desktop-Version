# AI provider configuration
# Use OpenRouter (free-tier friendly) or fallback to OpenAI if desired

# OpenRouter API key (recommended for free models)
OPENROUTER_API_KEY=

# Optional: Override the base URL (defaults to https://api.openrouter.ai/v1)
AI_API_BASE_URL=https://api.openrouter.ai/v1

# Select the model (defaults vary by provider)
# Good free option: meta-llama/llama-3.1-8b-instruct
AI_MODEL=meta-llama/llama-3.1-8b-instruct

# Fallback: OpenAI API key (used if OPENROUTER_API_KEY is not set)
OPENAI_API_KEY=